import React from "react";
import { motion } from "framer-motion";

export default function GameGenrePage() {
  return (
    <motion.div exit={{ opacity: 0 }}>this is the game genre page</motion.div>
  );
}
